{% extends "base.layout.njk" %}

{% block main %}
<div class="dark:text-white">
	<h2 class="font-black text-4xl tracking-tight">All Projects</h2>
	<p class="mt-4 mb-10">
		These are a collection of code snippets I've used in the past and saved.
		Some are Serverless Functions, which include set up instructions. Others
		are anything from random CSS snippets to Node.js scripts.
	</p>

	<div>
		<div class="flex flex-col my-2 mt-4 space-y-6">
			{% for project in collections.projects %}
			<a
				class="border-[2px] border-emerald-400 rounded p-4 w-full bg-white dark:bg-transparent"
				href="{{ project.url }}"
			>
				<h3 class="text-lg font-bold text-left">
					{{ project.data.title }}
				</h3>
				<p class="mt-1">{{ project.data.sub }}</p>
			</a>
			{% endfor %}
		</div>
	</div>
</div>
{% endblock %}
